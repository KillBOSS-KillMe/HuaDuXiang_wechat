
<template>
  <view class="page">
    <view class="channels-nav">
      <repeat for="{{navArr}}" item="item">
        <view
          class="nav-item {{navIdx == index ? 'nav-active':''}}"
          @tap="changeNav({{index}})"
        >{{item}}</view>
      </repeat>
    </view>
    <!-- 品牌 -->
    <view class="channels-cont" wx:if="{{navIdx == 1}}">
      <view class="brand-list">
        <repeat for="{{9}}" item="item">
          <view class="brand-item">
            <image class="img" src="/assets/img/image.png" />
            <text>兰蔻</text>
          </view>
        </repeat>
      </view>
    </view>
  </view>
</template>

<script>
import wepy from 'wepy';

export default class Index extends wepy.page {
  config = {
    navigationBarTitleText: '更多频道'
  };
  components = {};

  mixins = [];

  data = {
    navArr: ['分类', '品牌', '推荐商户'],
    navIdx: 1
  };

  computed = {};

  events = {};

  onLoad(options) {}
  onShow() {}
  methods = {
    changeNav(idx) {
      this.navIdx = idx;
    }
  };
}
</script>

<style lang="less">
.page {
  .channels-nav {
    position: fixed;
    z-index: 999;
    top: 0;
    width: 100%;
    display: flex;
    height: 84rpx;
    align-items: center;
    .nav-item {
      text-align: center;
      flex: 1;
      font-size: 30rpx;
      font-weight: bold;
      position: relative;
      height: 100%;
      line-height: 84rpx;
    }
    .nav-active {
      color: #ff2228;
      &::after {
        position: absolute;
        content: '';
        width: 50rpx;
        height: 6rpx;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        background: #ff2228;
      }
    }
  }
  .channels-cont {
    padding-top: 84rpx;
    .brand-list {
      display: flex;
      flex-wrap: wrap;
      padding: 0 30rpx;
      .brand-item {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        margin: 30rpx 70rpx 20rpx 0;
        &:nth-of-type(4n) {
          margin-right: 0;
        }

        .img {
          width: 120rpx;
          height: 120rpx;
          border-radius: 10rpx;
        }
        text {
          font-weight: bold;
          padding-top: 10rpx;
        }
      }
    }
  }
}
</style>